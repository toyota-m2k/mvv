<UserControl
    x:Class="wvv.WvvTrimmingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wvv"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Loaded="OnLoaded"
    Unloaded="OnUnloaded"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Width="400" Height="400" x:Name="mPlayerContainer">
            <MediaPlayerElement
                x:Name="mPlayerElement" 
                AutoPlay="False"
                HorizontalAlignment="Stretch" 
                VerticalAlignment="Stretch"
                Stretch="UniformToFill"
                AreTransportControlsEnabled="False" 
                Width="{Binding PlayerSize.Width}"
                Height="{Binding PlayerSize.Height}"
                />
        </Grid>
        <Grid Grid.Row="1" Margin="0,5">
            <local:WvvTrimmingSlider
                TotalRange="{Binding TotalRange}"
                TrimStartChanged="OnTrimStartChanged"
                TrimEndChanged="OnTrimEndChanged"
                CurrentPositionChanged="OnCurrentPositionChanged"
                />
        </Grid>
    </Grid>
</UserControl>
